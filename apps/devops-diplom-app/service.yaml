apiVersion: v1
kind: Service
metadata:
  name: devops-diplom-app
  namespace: cicd
  labels:
    app: devops-diplom-app
spec:
  type: NodePort
  selector:
    app: devops-diplom-app
  ports:
    - name: http
      port: 80
      targetPort: 80
